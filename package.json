{
  "name": "license-key-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel serverless bridge between a private GitHub repo (database.json) and license-key clients/admin UI",
  "engines": {
    "node": ">=18"
  }
}
